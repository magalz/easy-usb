FROM ubuntu:24.04
RUN apt-get update && apt-get install -y \
    linux-tools-generic linux-tools-common kmod iproute2 \
    && rm -rf /var/lib/apt/lists/*
COPY docker/entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh
EXPOSE 3240
ENTRYPOINT ["/entrypoint.sh"]
